/**
 * Notification service to send email/SMS notifications.
 * Replace the console.log calls with actual integration as needed.
 */
function sendNotification(recipient, message) {
  console.log(`Sending notification to ${recipient}: ${message}`);
  // Implement your email or SMS sending logic here.
}

module.exports = { sendNotification };
